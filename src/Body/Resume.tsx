import { Box, Typography } from "@mui/material"

export const Resume = () =>{
    
    return(
        <Box>
            <Typography fontSize={ 50 }>
                Resume
            </Typography>
        </Box>
    )
}