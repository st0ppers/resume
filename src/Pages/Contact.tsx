import { Box, Typography } from "@mui/material"

export const Contact = () =>{
    
    return(
        <Box>
            <Typography fontSize={ 50 } color={"red"}>
            Contact
            </Typography>
        </Box>
    )
}